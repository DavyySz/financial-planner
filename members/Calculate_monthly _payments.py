from symtable import Class

from django.db import models
from django.contrib.auth.models import User
from django.contrib.postgres.fields import ArrayField

class Calculate_monthly_payments(attributes):
    for x in attributes:
        def __init__(self,x):
            total_deposit_period = x.How_many_months_have_passed_since_the_category_was_created

        def Change_monthly_payment_amount(self, total_deposit_period):
            pass



